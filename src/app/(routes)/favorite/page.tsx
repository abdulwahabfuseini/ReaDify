import React from 'react'
import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Readify | Favorite",
    description: "Your Favorite Book",
  };

const Favorite = () => {
  return (
    <div>Favorite</div>
  )
}

export default Favorite